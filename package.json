{
  "name": "pong-arcade",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "pretest": "node -e \"require('fs').existsSync('node_modules')||require('child_process').execSync('npm install --no-audit --loglevel=error',{stdio:'inherit'})\"",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.0",
    "vitest": "^2.0.5"
  }
}
